#!/bin/bash

set -e
set -o pipefail

OUT="ckgen.out"
DATA="ckgen.data"

cd "/cygdrive/c/Program Files (x86)/Steam/steamapps/common/Fallout 4" || exit -1

for i in main ints other; do
	find "$OUT" -type f -name "*${i}*.esp" -print0 \
		| xargs -tr0 cp -pt "$DATA/$i"
done
